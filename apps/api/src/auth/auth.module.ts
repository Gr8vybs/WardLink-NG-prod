import { Module } from "@nestjs/common";

/**
 * AuthModule
 * Individual login (JWT, facility-scoped claims) + shared-device sessions
 * with per-action PIN re-auth. Placeholder — providers to be added.
 */
@Module({})
export class AuthModule {}
