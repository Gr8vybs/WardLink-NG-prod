# Infrastructure

Intentionally empty for now. Planned: Postgres (managed), object storage
(S3-compatible) with per-facility KMS keys, container hosting for the API,
and CI/CD wiring — provisioned as code from the start so a future move to
per-facility backend deployments (if this ever goes beyond portfolio scope)
is a scaling decision, not a rewrite.
